package com.birds.objects;

/** Creates a DefaultConservatory to extend Conservatory so that it uses same formatting. */
public class DefaultConservatory extends Conservatory {
  /** Creates a constructor for DefaultConservatory. */
  public DefaultConservatory() {
    super();
  }
}
