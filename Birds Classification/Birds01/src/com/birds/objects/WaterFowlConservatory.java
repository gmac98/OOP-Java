package com.birds.objects;

/** Creates a WaterFowlConservatory to extend Conservatory so that it uses same formatting. */
public class WaterFowlConservatory extends Conservatory {
  /** Creates a constructor for WaterFowlConservatory. */
  public WaterFowlConservatory() {
    super();
  }
}
