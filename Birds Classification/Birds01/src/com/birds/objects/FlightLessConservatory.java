package com.birds.objects;

/** Creates a FlightlessConservatory to extend Conservatory so that it uses same formatting. */
public class FlightLessConservatory extends Conservatory {
  /** Creates a constructor for FlightlessConservatory. */
  public FlightLessConservatory() {
    super();
  }
}
